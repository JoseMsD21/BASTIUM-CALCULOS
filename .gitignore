.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.db
*.db.bak-*
*.sqlite3

# Respaldos manuales de bastium.db, organizados aca desde el Sprint 64
# (ya cubiertos por *.db.bak-* de arriba independientemente de la carpeta,
# pero se declara explicito para que la convencion quede documentada)
backups/

# Salidas generadas por corridas de ejemplo (main.py)
/Liquidacion_Bastium.pdf
/distribucion.png
exports/*.pdf

# Worktrees aislados para desarrollo (superpowers)
.worktrees/

# Mockups del companero visual de brainstorming (superpowers)
.superpowers/

# Configuracion local y worktrees efimeros de Claude Code (no compartidos en el repo)
.claude/

# Notas personales de uso local (guias para reuniones, etc.) - nunca al repo
docs/local/

# Plantillas del despacho (Ediciones Sistematizadas Equidad, con copyright que
# prohibe reproduccion/comercializacion) y casos reales de clientes con datos
# identificables (ej. liquidacion_cliente_ejemplo.xlsx) - repo es publico,
# nunca subir esto
docs/Archivos de referencia abogado/
